<UserControl x:Class="InventoryManagamentSystem_WPF_DB.Views.AddProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventoryManagamentSystem_WPF_DB.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Grid.IsSharedSizeScope="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Add Product" Style="{StaticResource Heading}" HorizontalAlignment="Center"/>
        
        <!--Name-->
        <Grid Grid.Row="1" Margin="0, 16, 0, 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Name:"/>
            <TextBox Text="{Binding Name}" Grid.Row="2"/>
        </Grid>

        <!--Price & Quantity-->
        <Grid Grid.Row="2" Margin="0, 16, 0, 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" SharedSizeGroup="FirstColumn"/>
                <ColumnDefinition Width="24"/>
                <ColumnDefinition Width="auto" SharedSizeGroup="SecondColumn"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Price:"/>
            <TextBox Text="{Binding Price}" Grid.Row="2"/>

            <TextBlock Text="Quantity:" Grid.Column="2"/>
            <TextBox Text="{Binding Quantity}" Grid.Row="2" Grid.Column="2"/>
        </Grid>
        
        <!--Category-->
        <Grid Grid.Row="3" Margin="0, 16, 0, 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Category"/>
            <ComboBox Grid.Row="2" ItemsSource="{Binding ProductCategory}"/>
        </Grid>
        
        <!--Dynamic Content Control-->
        <ContentControl Grid.Row="4" Margin="0, 16, 0, 0" Content="{Binding DynamicContentControl}"/>

        <StackPanel Orientation="Horizontal" Grid.Row="5">
            <Button Content="Add product" Command="{Binding AddProductCommand}" Margin="0, 0, 24, 0"/>
            <Button Command="{Binding CancelCommand}" Content="Cancel"/>
        </StackPanel>
    </Grid>
</UserControl>
