<UserControl x:Class="InventoryManagamentSystem_WPF_DB.Views.RemoveProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventoryManagamentSystem_WPF_DB.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Remove Product" Style="{StaticResource Heading}" HorizontalAlignment="Center"/>
        <Grid Grid.Row="1" Margin="0, 24, 0, 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Product ID:" Margin="0, 0, 0, 8"/>
                <TextBox Text="{Binding ProductID}" Width="256" HorizontalAlignment="Left"/>
            </StackPanel>

            <Button Grid.Column="1" Command="{Binding SearchCommand}" Content="Search"/> 
        </Grid>

        <ContentControl Grid.Row="2" Margin="0, 16, 0, 0"/>

        <StackPanel Orientation="Horizontal" Grid.Row="3" Margin="0, 16, 0, 0">
            <Button Content="Remove product" Command="{Binding RemoveProductCommand}" Margin="0, 0, 24,0"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
