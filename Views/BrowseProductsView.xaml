<UserControl x:Class="InventoryManagamentSystem_WPF_DB.Views.BrowseProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventoryManagamentSystem_WPF_DB.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Browse Products" Style="{StaticResource Heading}"/>
            <Button Grid.Column="1" Content="Return" Command="{Binding ReturnCommand}"/>
        </Grid>

        <!--TODO: Add binding-->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0, 32, 0, 0">
            <RadioButton Content="All products" Margin="0, 0, 8, 0" IsChecked="True"/>
            <RadioButton Content="Electornic products" Margin="0, 0, 8, 0"/>
            <RadioButton Content="Perishable goods" Margin="0, 0, 8, 0"/>
            <RadioButton Content="Clothing products" Margin="0, 0, 8, 0"/>
        </StackPanel>

        <!--TODO: Add dynamyc listview - delete this one-->
        <Grid Grid.Row="2" Margin="0, 24, 0, 0">
            <ListView>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" Width="100" DisplayMemberBinding="{Binding ProductID}"/>
                        <GridViewColumn Header="Name" Width="100" DisplayMemberBinding="{Binding Name}"/>
                        <GridViewColumn Header="Category" Width="100" DisplayMemberBinding="{Binding Category}"/>
                        <GridViewColumn Header="Price" Width="100" DisplayMemberBinding="{Binding Price}"/>
                        <GridViewColumn Header="Quantity" Width="100" DisplayMemberBinding="{Binding Quantity}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </Grid>
</UserControl>
